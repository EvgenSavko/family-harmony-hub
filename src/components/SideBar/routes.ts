import InboxIcon from '@mui/icons-material/MoveToInbox';
import MailIcon from '@mui/icons-material/Mail';

const routes = [
  { path: '/home', label: 'Home', icon: InboxIcon },
  { path: '/about', label: 'About', icon: MailIcon },
];
const routes2 = [{ path: '/contact', label: 'Contact', icon: InboxIcon }];

export { routes, routes2 };
